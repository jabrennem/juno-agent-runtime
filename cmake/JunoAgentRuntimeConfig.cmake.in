@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
# nlohmann_json is a header-only implementation detail. Its headers are
# installed alongside the SDK, so consumers do not need a separate package.
include("${CMAKE_CURRENT_LIST_DIR}/JunoAgentRuntimeTargets.cmake")
